public class Readme {

    //Teoría:
    //¿Cuál es la diferencia entre una clase y un objeto?
        //RTA:
            //Una clase es una plantilla para crear objetos. Un objeto es una instancia de una clase.
            //La clase puede tener un método initialize llamado constructor que se va a llamar cada vez que
            // se cree un objeto de esa clase.
    //2. Indique los pasos para versionar en GitHub.
        //RTA:
            //1. Crear un new_repo en Github
            //2. Clonar el repo en la carpeta local del proyecto en el pc (Git clone + Url del new_repo en git)
            //3. git status (valida que no se han commit)
            //4. git add . (añade un cambio del directorio de trabajo en el entorno de ensayo.)
            //5. git commit -m "xxx" ( captura una instantánea de los cambios preparados en ese momento del proyecto)
            //6. git push (se usa para cargar contenido del repositorio local a un repositorio remoto)

}
